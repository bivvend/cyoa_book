LLM  methods to generate long fantansy stories.  
Uses enforced structured output in JSON.
Tools to configure an author agent with a large vector set to help keep the story on track.

Uses OpenAI API
